SELECT event_id from users_events
where user_id = 1;

Vypíše ID všetkých eventov(moje eventy) na ktoré je daný user prihlasený (user_id ide meniť podľa toho pre ktorého usera hľadáme )


SELECT events.name from events
where events.id = 1;

Vypíše názov eventu podľa toho aké dostane ID (Moje eventy)



/////otvorenie určitého eventu v moje eventy


SELECT image from events
where events.id = 1;

Q na obrázok eventu 


SELECT category.name from events
join category on events.category=category.categ_id
where events.id = 1;

Vypiše názov kategorie eventu podľa toho aké ID eventu zadáme 

SELECT date from events
where events.id = 1;

Vypíše čas eventu podľa ID 



SELECT cities.name from events
join cities ON events.city=cities.city_id
where events.id = 1;

Vypíše mesto v ktorom sa koná event 


